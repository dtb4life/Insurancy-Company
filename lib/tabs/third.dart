import 'package:flutter/material.dart';

class ThirdTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Заключенных договоров пока нет',
        style: TextStyle(
          fontSize: 20.0,
        ),
      ),
    );
  }
}
